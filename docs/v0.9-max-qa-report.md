# v0.9 Max 真实浏览器 QA 报告

验收日期：2026-07-18 至 2026-07-20

候选版本：`0.9.2` / 建议 tag `v0.9.2-mvp`
验收基线：`main@dd69e74` 加演示就绪度优化

## 结论

**Conditional GO**：本地单用户 MVP 的核心流程和基础故障边界已跑通，真实分析数值与标准答案一致。验收发现的两个 P0 和一个 P1 已修复并完成回归。固定演示首问和追问均连续两次成功。WebSocket 在服务中断超过自动重试窗口后需要点击一次“重连”；标签页关闭会取消当轮任务，但历史内容不丢失，重新打开后可以再次提交。两项均作为 v0.9 已知降级路径接受。

本结论不代表企业级生产可用，不包含鉴权、多租户、部署、监控、压测或安全完整审计。

## 自动化基线

| 项目 | 结果 |
|---|---:|
| Python | 3.12.1 |
| pytest | **299 passed** |
| React/TypeScript production build | **passed** |
| `/api/health` | **200 / `{\"status\":\"ok\"}`** |
| `git diff --check` | passed |
| Pi 子进程 Git 防线 | `GIT_DIR=/dev/null` 下 Git 被阻断 |

## 真实浏览器核心旅程

测试文件：`docs/test_datasets/simple/01_门店月度销售.xlsx`

固定问题：分析 2025 年上半年各门店总销售额，给出从高到低的完整排名，生成一张横向条形排名图，并导出包含排名、门店和总销售额的 CSV 与 Excel。

| 检查项 | 结果 |
|---|---|
| 首页选择并上传 `.xlsx` | PASS |
| 创建会话并建立 WebSocket | PASS |
| 展示思考过程与单步执行计划 | PASS |
| 最终报告渲染 | PASS |
| 数值正确性 | PASS：杭州西湖店 753.3；北京旗舰店 739.7；上海南京路店 664.9；广州天河店 664.8；成都春熙路店 566.7 |
| CSV / XLSX 产物登记 | PASS |
| 表格预览 | PASS：5 行完整显示 |
| 下载事件 | PASS |
| 同会话追问 | PASS：Top 2 为杭州 753.3、北京 739.7；分别比第三名高 88.4、74.8 |
| 追问 CSV | PASS：2 行预览并可下载；UI 数值格式整洁 |
| 刷新恢复 | PASS：两轮报告、消息与产物均恢复 |
| 跨服务重启恢复 | PASS with workaround：自动重试窗口耗尽后手动点击“重连”恢复 |

连续彩排证据：首问两轮分别耗时 48.2 秒和 83.0 秒；固定追问两轮成功运行分别耗时 114.9 秒和 54.1 秒。第一次追问在浏览器连接中断时于 47.6 秒被明确标为 cancelled；重新打开历史会话后恢复成功，没有半成品被标记为完成。

演示尺寸、六张真实截图和断线恢复记录见 `docs/demo/v0.9-demo-visual-qa.md`。

## 故障路径

| 场景 | 期望 | 结果 |
|---|---|---|
| 上传 `README.md` | 前端拒绝，不创建会话 | PASS：“目前请上传 .xlsx 或 .xlsm 文件” |
| 上传伪造损坏 `.xlsx` | 明确报错，留在首页 | PASS：“Excel 文件无法读取，请确认文件未损坏且未加密” |
| 文件已选但问题仅为空格 | 不能开始 | 初测 FAIL；修复后 PASS |
| 运行中点击停止 | 状态为 cancelled，连接可继续用 | PASS |
| 运行中删除会话 | 409，不清 workspace | PASS |
| 取消后删除会话 | 204，清理 workspace/session | PASS |
| 故障后健康检查 | 服务仍健康 | PASS |

## 验收发现及修复

### P0-1：数据分析 Agent 越权执行 Git

首次真实运行中，Pi 默认读取仓库 `CLAUDE.md`，把“每次有改动后都需要 Git 提交”的编码规则错误套用到数据分析任务，执行了 `git add -A && git commit`。该提交已用 mixed reset 安全撤销，所有原文件完整保留为未跟踪状态。

修复：

- Pi sidecar 强制加入 `--no-context-files`，隔离 `CLAUDE.md` / `AGENTS.md`。
- 默认只启用调用 typed tool service 所需的 `bash`。
- Pi 子进程强制 `GIT_DIR=/dev/null`，阻断仓库发现与写入。
- runtime prompt 明确禁止 Git、修改源码/文档、安装依赖和提交。
- 新增回归测试验证命令行、环境和 prompt 防线。

复测：同会话真实追问成功，报告/CSV 正常，仓库 `HEAD` 未变化。

### P0-2：`make run --reload` 被运行时脚本误触发

原 `make run` 监控整个仓库。Agent 在 `workspace/<session>/scripts/` 生成 Python 脚本时会触发 Uvicorn reload，导致 WebSocket 中断。

修复：默认 `make run` 改为稳定无 reload；新增 `make run-dev`，只监控 `app/`。

### P1-1：空白问题仍可点击开始

首页只检查文件，未对 query 执行 `trim()`。已在提交入口、mutation 和按钮 disabled 条件中统一加入空白守卫；最新生产构建通过真实浏览器复测。

## 接受的 v0.9 限制

- SheetBench complex-qa 为 15/21；104、101、126、2292、1135、515 作为分析语义准确性限制，不在 v0.9 临交付阶段追分。
- WebSocket 自动重试为 1s/2s/4s 三次；超出窗口后需要手动点击“重连”。
- CSV/XLSX 的 UI 预览已清理二进制浮点尾差；个别 Agent 生成 CSV 的原始文本仍可能保留 `753.3000000000001` 一类精度尾差，业务结论正确，列为 P2 导出格式问题。
- follow-up 会对已存在的同路径产物重复登记 SQLite artifact 行；UI 会按路径去重，不影响本轮展示和下载，列入后续幂等持久化与 GC。
- 当前只面向本地/内网单用户，不包含生产级安全与运维能力。

## 发布前最后动作

1. 精确 stage 演示优化、真实截图与交付文档，排除无关 docx/业务生成脚本和早期脏截图。
2. 创建演示就绪度 commit。
3. annotated tag `v0.9.2-mvp` 与远端 push 仍需单独确认。

最终 Gate（2026-07-20）：`299 passed`、production build passed、演示预检 10/10、`git diff --check` passed。

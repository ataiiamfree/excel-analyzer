# ChatExcel v0.9 Max 发布与现场演示交接

适用版本：ChatExcel v0.9 Max（0.9.2）

这份文件只做交接，不替演示者执行 GitHub 发布，也不移动或删除仓库中的个人材料。

## 一、可直接发给 CC 的 GitHub 发布提示词

请在 `/Users/natalia/test/excel-analyzer` 仓库完成 ChatExcel v0.9 Max（0.9.2）的安全发布。严格按以下要求执行：

1. 先检查当前分支、工作区、远端和 tag：

   ```bash
   git status --short --branch
   git branch --show-current
   git remote -v
   git tag --list --sort=version:refname
   git fetch origin --prune
   git rev-list --left-right --count origin/main...main
   ```

2. 预期当前分支是 `main`。历史基线中，本地 `main` 比 `origin/main` 多两个已验证提交：

   - `dd69e74 release: harden v0.9.2 MVP candidate`
   - `fdc3740 demo: prepare v0.9 Max presentation package`

   fetch 后如果 `origin/main` 出现新提交、双方分叉，或上述事实不再成立，立即停止并报告，不要 force push、reset、rebase 或覆盖远端。

3. 不要执行 `git add -A`、`git add .`，不要提交下方“由用户手动移开”的材料。只审查并暂存本次交付文件：

   ```bash
   git add docs/demo/v0.9-deck-outline.md
   git add docs/demo/ChatExcel-v0.9-Max-Demo.pptx
   git add docs/demo/v0.9-release-and-demo-handoff.md
   git diff --cached --stat
   git diff --cached --check
   ```

4. 在提交前复跑质量 Gate；使用仓库现有环境，不要升级依赖：

   ```bash
   python -m pytest tests/ -q
   npm --prefix web run build
   python scripts/demo_preflight.py
   ```

   预期：Python tests 299/299、前端 production build 成功、demo preflight 10/10。若环境中的 Python 命令不同，可使用仓库已有虚拟环境里的 Python，但不要改依赖版本。

5. 只有全部通过后，提交本次 PPT/交付文档：

   ```bash
   git commit -m "docs: add v0.9 Max engineering presentation"
   ```

   如果这些文件已经由等价提交纳入历史，不要制造空提交。

6. 再次确认 `git status` 和提交历史，只把 `main` 正常 push 到 `origin/main`：

   ```bash
   git status --short --branch
   git log --oneline --decorate -6
   git push origin main
   ```

   禁止使用 `--force` 或 `--force-with-lease`。

7. push 成功并确认远端 `main` 指向最终验证提交后，检查本地与远端是否已有 `v0.9.2-mvp`：

   ```bash
   git tag --list v0.9.2-mvp
   git ls-remote --tags origin refs/tags/v0.9.2-mvp refs/tags/v0.9.2-mvp^{}
   ```

   若 tag 已存在，停止并报告，不要移动或覆盖。若不存在，在最终验证提交上创建 annotated tag：

   ```bash
   git tag -a v0.9.2-mvp -m "ChatExcel v0.9.2 MVP"
   git push origin v0.9.2-mvp
   ```

8. 最后报告：

   - 当前分支
   - `origin/main` 最终 commit SHA
   - `v0.9.2-mvp` 指向的 commit SHA
   - push 与 tag 是否成功
   - 仍未跟踪的文件清单

整个过程中保留用户的未跟踪文件，不移动、不删除、不自动归档；发现不确定项时停止并说明。

## 二、现场演示只需要这组文件

### 固定输入

- Excel：`docs/test_datasets/simple/01_门店月度销售.xlsx`
- 演示 runbook：`docs/demo/v0.9-demo-runbook.md`
- 开场前检查：`docs/demo/v0.9-demo-checklist.md`
- 独立标准答案：`docs/demo/v0.9-demo-expected-results.md`
- 真实浏览器 QA：`docs/demo/v0.9-demo-visual-qa.md`
- 90–120 秒录屏脚本：`docs/demo/v0.9-video-script.md`
- 汇报 PPT：`docs/demo/ChatExcel-v0.9-Max-Demo.pptx`

### 固定主问题

> 分析 2025 年上半年各门店总销售额，给出从高到低的完整排名，生成一张横向条形排名图，并导出包含排名、门店和总销售额的 CSV 与 Excel。

### 固定追问

> 只保留销售额最高的两个门店，并说明它们分别比第三名高多少，输出 CSV。

不要临场改写这两句话。

### 现场核对数字

- 杭州西湖店：753.3 万元
- 北京旗舰店：739.7 万元
- 上海南京路店：664.9 万元
- 广州天河店：664.8 万元
- 成都春熙路店：566.7 万元
- Top 1 / Top 2 相对第三名：88.4 / 74.8 万元

首问必须有完整五店排名、横向图、5 行 CSV 和 5 行 Excel；追问必须有两家门店、88.4/74.8 和 2 行 CSV。最后刷新页面，确认消息、报告和产物恢复。

### 开场前命令

```bash
make web-build
make run
python scripts/demo_preflight.py
```

预检必须为 10/10。现场运行最长可能接近 2 分钟；保留一个用相同工作簿和相同问题跑完并核对过的备用会话。网络异常时明确说：

> 网络暂时不可用，我切到刚才已经完成并核对过的同一分析，继续展示报告和产物。

不要把备用会话称为刚刚实时生成的结果。

### 已有真实 QA

- Python tests：299/299
- React/TypeScript production build：PASS
- Demo preflight：10/10
- 两轮固定主线：首问与追问均得到正确业务结果
- 首问耗时：48.2 秒、83.0 秒
- 追问成功耗时：114.9 秒、54.1 秒
- 1440×900 与 1920×1080：PASS
- SheetBench：15/21 答案正确、21/21 稳定收口、0 unhandled exception

## 三、由用户手动移开的材料

以下项目当前均未被 Git 跟踪，产品运行不依赖它们。它们大多是 CC 文档、个人材料、DOCX 生成过程或早期截图。请由用户自行移到仓库外；不要让 CC 用 `git clean` 或批量删除。

### 计划和状态文档

- `.hermes/plans/2026-07-15_094348-sheetbench-pr4-acceptance-and-next-benchmarks.md`
- `.hermes/plans/2026-07-15_144155-friday-sheetbench-delivery-plan.md`
- `docs/Delivery-Plan-MVP.md`
- `docs/MVP-Execution-Steps.md`
- `docs/execution-status.md`

### DOCX 制作过程目录和脚本

- `docs/.production_case_work/`
- `docs/build_production_log_case_doc.py`
- `docs/build_production_log_case_from_template.py`
- `docs/inspect_case_docx.py`
- `docs/rebuild_production_log_final_extraction.py`

### 个人 DOCX 材料

- `docs/以统一业务语言破解多源数据协同难题推动生产日志自动化落地-修订扩展版.docx`
- `docs/经验萃取表-生产指挥日报自动化-问题解决类.docx`
- `docs/经验萃取表-生产日志自动化-问题解决类-最终整理版.docx`
- `docs/陈虹伊-以统一业务语言破解多源数据协同难题推动生产日志自动化落地.docx`

### 早期截图

- `docs/demo/screenshots/01-home-1440x900.png`

这张早期截图含无关测试会话，不进入 PPT 或正式演示。正式首页截图是：

- `docs/demo/screenshots/01-home-clean-1440x900.png`

### 过期诊断分支

- 分支：`codex/sheetbench-1135-diagnosis`
- 旧 worktree metadata：`/private/tmp/excel-analyzer-diag-1135`

诊断提交的等价改动已进入 `main`，但分支和 worktree metadata 不影响本次产品运行。若要清理，请另行确认后再做；本交接不执行删除。
